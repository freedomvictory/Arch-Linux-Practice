# SSH and SFTP 


## Instruction 



## How to configure `sshd`





## how the sftp client connect to server 


1. Install the `sshfs` tools 
2. Run the command : 

`sshfs [user@]host:[dir] mountpoint [options]`

***For example:***

 `sshfs myuesr@mycomputer:/remote/path /local/path -C -p 22`

